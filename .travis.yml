language: node_js

services:
  - docker

jobs:
    include:
        - stage: instal dependencies
          script: npm ci
        - stage: build
          script: npm run build
        - stage: build docker image
          script: docker build . -t "$VERSION"